board:
aux[2] pos;
uMap
tMap

public:
string printMap();
string showRange(unit); //print checkValidMoves(); o print checkTargetsStatic();
int[][] checkValidMoves(unit);  -1 para obstaculos y otras unidades,
                                0 para el resto, contamos de mov hacia atrás hasta llegar a 1
                                -2 para targets validos

bool[][] checkTargetsStatic(unit);



check end

turn(int player)
	do 2 veces
		if hay peones moveP > 1
			list peones moveP > 1
			while attP > 1
			moveAndAttack(peon)

	lo mismo con los otros, 1 vez

startGame


Board.loadArmy();